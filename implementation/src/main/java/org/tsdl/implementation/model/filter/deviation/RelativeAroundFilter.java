package org.tsdl.implementation.model.filter.deviation;

/**
 * An {@link AroundFilter} that specifies its {@link AroundFilter#maximumDeviation()} as a relative (percentage) difference to its
 * {@link AroundFilter#referenceValue()}.
 */

public interface RelativeAroundFilter extends AroundFilter {
}
